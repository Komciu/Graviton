add_executable(EntityTests AllTests.cpp)
if(UNIX)
	target_link_libraries(EntityTests EntityLib SimLib gtest gtest_main pthread)
else(WIN32)
	target_link_libraries(EntityTests EntityLib SimLib gtest gtest_main)
endif()
